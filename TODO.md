Check store module and related codebase, documentation are needed.
As Vendor need store to be created and update , deleted.
As Admin needs to view stores and products under specified vendor.


On Store Creation pass vendor Id as now something else is happen
this give concert logic as store is created under same vendor id
